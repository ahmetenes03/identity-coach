fastapi==0.115.6
uvicorn[standard]==0.34.0
SQLAlchemy==2.0.36
psycopg[binary]>=3.2,<4.0
alembic==1.14.0
pydantic-settings==2.7.0
email-validator==2.2.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-jose[cryptography]==3.3.0
python-dotenv==1.0.1
pytest==8.3.4
httpx==0.28.1

# --- Optional AI providers -------------------------------------------------
# The default "offline" provider needs none of these. Install only the SDK for
# the provider you configure via LLM_PROVIDER. Ollama uses httpx (already above).
# google-genai>=1.0.0        # LLM_PROVIDER=gemini
# openai>=1.0.0              # LLM_PROVIDER=openai
# anthropic>=0.40.0          # LLM_PROVIDER=claude
