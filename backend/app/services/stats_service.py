# TODO: Stats service - istatistik hesaplamaları
# - get_user_stats(user_id) -> overall completion rate
# - get_weekly_stats(user_id) -> haftalık breakdown
# - get_recurring_excuses(user_id) -> en sık bahaneler
