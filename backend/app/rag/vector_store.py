# Sprint 2 TODO: pgvector VectorStore
# class VectorStore:
#     search_similar_strategies(embedding, limit) -> list[dict]
#         SQL: ORDER BY embedding <=> :embedding
#     search_similar_failures(user_id, embedding, limit)
#     store_embedding(...)
